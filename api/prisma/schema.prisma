// 1. BLOK GENERATOR
generator client {
  provider = "prisma-client-js"
}

// 2. BLOK DATASOURCE (HARUS KOSONG DARI URL DAN SCHEMA)
datasource db {
  provider = "postgresql" 
  // HAPUS BARIS INI: url = env("DATABASE_URL")
  // HAPUS BARIS INI: schema = ".."
}

// 3. MODEL ANDA
model Room {
  id          Int      @id @default(autoincrement())
  roomNumber  String   @unique
  roomType    String
  price       Float
  isAvailable Boolean  @default(true)
  capacity    Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}